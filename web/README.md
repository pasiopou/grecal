# Website source

This directory is reserved for the hand-written HTML, CSS, and JavaScript for
the Grecal website. The frontend will be added in the next website milestone.

Run `python scripts/build_site.py` from the repository root to generate the
website data and subscription feeds under `_site/`. Generated artifacts are
ignored by Git and should not be edited by hand.
